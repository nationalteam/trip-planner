services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - db_data:/app/data
    restart: unless-stopped

volumes:
  db_data:
